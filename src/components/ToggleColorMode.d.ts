import * as React from 'react';
import { PaletteMode } from '@mui/material';
interface ToggleColorModeProps {
    mode: PaletteMode;
    toggleColorMode: () => void;
}
declare function ToggleColorMode({ mode, toggleColorMode }: ToggleColorModeProps): React.JSX.Element;
export default ToggleColorMode;
//# sourceMappingURL=ToggleColorMode.d.ts.map